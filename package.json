{
  "name": "playwright",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "generate-excel": "node scripts/generate-excel.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "xlsx": "^0.18.5"
  }
}
